<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/highlight</item>
        <item name="colorPrimaryDark">@color/seaBlue</item>
        <item name="colorAccent">@color/gray</item>
        <item name="actionBarStyle">@style/Widget.MaterialComponents.AppBarLayout.Primary</item>
    </style>

    <!-- Custom ActionBar style -->
    <style name="MyActionBar" parent="@style/ShapeAppearance.Material3.Corner.ExtraLarge">
        <item name="background">@color/seaBlue</item>
    </style>

</resources>